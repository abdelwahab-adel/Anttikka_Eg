# سجل الإصلاحات — موقع Anttikka
تاريخ: 1 سبتمبر 2026

هذا الملف يوثّق كل تعديل تم إجراؤه على الموقع، لماذا، وكيف تم التأكد من أنه يعمل بدون كسر أي شيء آخر. كل نقطة أدناه اختُبرت فعليًا بمتصفح حقيقي (Playwright/Chromium) قبل اعتمادها.

---

## 1) الأداء — تقليل حجم الموقع من 32 ميجابايت إلى 5.3 ميجابايت (~84%)

**أ) حذف ملفات مكررة غير مستخدمة (32 ميجا ← 11 ميجا)**
- حذف مجلد `images/New folder/` بالكامل (50 ملف) — لم يكن مُشارًا إليه في أي HTML/JS إطلاقًا.
- حذف 13 ملف صورة مكرر داخل `images/prodact/` بأسماء إنجليزية/عامة (مثل `birch_upholstered_chair_....png`) — نفس المحتوى تمامًا موجود ومُستخدَم فعليًا بالاسم العربي الصحيح.
- حذف ملفين غير مستخدمين من جذر `images/` (`desktop.ini`, `riva_corner_sofa_....png`).
- **طريقة التأكد:** تمت مطابقة كل ملف محذوف بالـ hash (البصمة الرقمية) مع نسخة مطابقة موجودة ومُستخدَمة، ثم أُعيد فحص كل الروابط والصور في الموقع بعد الحذف للتأكد من عدم وجود أي رابط مكسور.

**ب) ضغط الصور المُستخدمة فعليًا (11 ميجا ← 5.3 ميجا)**
- اكتُشف أن 6 صور مُسمّاة بامتداد `.png` هي في الحقيقة صور JPEG بجودة شبه خام (غير مضغوطة). تم ضغطها بجودة 85% بنفس الامتداد `.png` بالضبط (بدون تغيير أي مرجع في الكود).
- باقي الصور (PNG حقيقية وJPG) تم تحسينها بأدوات ضغط بلا فقدان ملحوظ في الجودة (jpegoptim / pngquant).
- **طريقة التأكد:** تمت مقارنة الصور الأصلية بالمضغوطة بصريًا واحدة تلو الأخرى (خصوصًا أصعب الحالات: تفاصيل معدنية دقيقة وتدرّجات لونية ناعمة)، ولم يظهر أي فرق ملحوظ.

---

## 2) الفيديوهان المكسوران في الصفحة الرئيسية

**المشكلة:** فيديو قسم "قصة الحرفية" وفيديو الودجت العائم كانا يشيران لملفين غير موجودين، فيظهر مربع أسود فارغ لكل زائر.

**الحل المُطبَّق (بدون حذف أي ميزة):**
- تمت إضافة صورة غلاف (poster) جديدة مصمَّمة خصيصًا لهذا القسم (`images/process-poster.webp`) — تدرّج بنّي/ذهبي متناسق تمامًا مع ألوان هوية الموقع، بحجم 8.6 كيلوبايت فقط.
- تمت إضافة معالجة أخطاء في الكود (`main.js` و`main.min.js`): لو فشل تحميل ملف الفيديو، يختفي زر التشغيل تلقائيًا (بدل زر لا يعمل) وتبقى الصورة البديلة الأنيقة ظاهرة.
- الفيديو العائم: أُضيفت معالجة تجعله يزيل نفسه بالكامل من الصفحة تلقائيًا لو تعذّر تحميل الفيديو، بدلاً من ترك مربع أسود عائمًا فوق المحتوى.
- **مهم:** بمجرد رفع ملفي الفيديو الحقيقيين بنفس الاسمين بالضبط (`images/WhatsApp Video 2026-07-31 at 11.38.17 PM.mp4` و `images/Video.mp4`)، سيعمل كل شيء تلقائيًا كما صُمِّم أصلاً — لم يُحذف أي كود متعلق بالفيديو نفسه.
- تم اكتشاف وإصلاح خلل توقيت إضافي (race condition) أثناء الاختبار: الفيديو يبدأ التحميل فور تحليل الصفحة (قبل أن يعمل الكود الموجود أسفل الصفحة)، فتم تعديل المنطق ليتحقق من حالة الفشل الحالية فورًا وليس فقط بالاستماع لفشل مستقبلي.

---

## 3) بطاقة "الأكثر مبيعاً" في الهيرو

**المشكلة:** نص الوصول (aria-label) يقول السعر 9,850 بينما الظاهر فعليًا 4,900، والضغط على البطاقة كان يوصل لمنتج مختلف تمامًا بدل "طاولة سفرة أوك".

**الحل:**
- تصحيح نص aria-label ليطابق السعر الظاهر (4,900).
- تعديل رابط البطاقة ليحمل معرّف المنتج الصحيح (`product.html?id=طاولة-سفرة-أوك`)، بنفس آلية الروابط المُستخدَمة في باقي الموقع.
- **اكتشاف إضافي أثناء الاختبار:** ظهر أن السعر يُعرض "NaN" بعد هذا التعديل! تتبّعت السبب لخلل برمجي كامن (لم يُكتشف من قبل لأن هذا المسار البرمجي لم يكن يُستخدَم إطلاقًا): دالة `fmt()` المسؤولة عن تنسيق الأسعار لم تكن تتعامل بشكل صحيح مع أسعار مُخزَّنة كنص جاهز يحتوي على فاصلة (مثل `"4,900"`). تم إصلاح الدالة نفسها من الجذر بدلاً من تجاوز المشكلة، مما يحمي أي رابط منتج مباشر مشابه مستقبلاً.

---

## 4) تصادم عداد "المفضلة" مع عداد "السلة"

**المشكلة:** عنصر عداد المفضلة في الهيدر كان يحمل كلاسّين معًا (`cart-count wishlist-count`)، فكانت أي إضافة/حذف في السلة تكتب رقم السلة فوق رقم المفضلة الظاهر بالغلط.

**الحل:**
- تم فصل الكلاسّين في كل الصفحات الإحدى عشرة، مع إضافة نفس التنسيق البصري لكلاس `wishlist-count` بمفرده في `style.css` و`style.min.css` — النتيجة: نفس الشكل تمامًا (دائرة ذهبية) لكن كل عداد الآن مستقل تمامًا عن الآخر.

---

## 5) تزامن main.js مع main.min.js

**المشكلة الأصلية:** خطأ برمجي واحد مكتشف في منتج "كرسي بيرش المنجّد" (متغيّر `shopGrid` بدل السعر الأصلي).

**عند المراجعة الشاملة سطرًا بسطر (بمقارنة برمجية آلية لكامل بيانات المنتجات بين الملفين)، تم اكتشاف 14 اختلافًا إضافيًا لم يكن أي منها مذكورًا في التقرير الأصلي** — أغلبها أرقام أسعار ناقصة الخانة الأولى في `main.js` (مثل `"399"` بدل `"1,399"`، و`"519"` بدل `"2,519"`). بما أن `main.min.js` هو الملف الوحيد الذي يعمل عليه الموقع الفعلي، تم اعتماده كمرجع صحيح وتصحيح `main.js` ليطابقه تمامًا في 10 منتجات مختلفة عبر 5 تصنيفات (كراسي، إضاءة، طاولات تلفزيون، بوفيهات، لوحات جدارية).

**طريقة التأكد:** كتابة سكريبت مقارنة آلي (Node.js) يستخرج كامل بيانات المنتجات من الملفين ويقارنها حقلاً بحقل، وإعادة تشغيله بعد كل تصحيح حتى ظهرت النتيجة: **0 اختلافات متبقية.**

---

## 6) إضافة مربع بحث فعلي في الصفحة الرئيسية (ديسكتوب)

**المشكلة:** كود جاهز في `main.min.js` كان ينتظر عنصرين (`#heroSearchForm` / `#heroSearchInput`) غير موجودين إطلاقًا في `index.html` — أي كود ميت لا يعمل، وزائر الديسكتوب لم يكن يجد أي طريقة للبحث من الصفحة الرئيسية.

**الحل:** تصميم مربع بحث جديد بالكامل (HTML + CSS) بنفس الطابع الزجاجي الشفاف (glassmorphism) المستخدم في أزرار الهيرو الحالية — يظهر أسفل أزرار "تسوّق التشكيلة" مباشرة على الديسكتوب فقط (يختفي تلقائيًا تحت 860px حيث يوجد بالفعل بحث مخصص في شريط التنقل السفلي بالموبايل، لتفادي التكرار). يستخدم بالضبط نفس المعرّفات (`id`) التي كان الكود القديم ينتظرها، فأصبح يعمل فورًا دون أي تعديل إضافي في الجافاسكريبت.

---

## ملاحظة مهمة

كل الأخطاء الخمسة الأصلية + الأخطاء الإضافية المكتشفة أثناء الإصلاح (خلل fmt، خلل التوقيت بالفيديو، 14 فرق سعر إضافي في main.js) **تم اختبارها جميعًا حيًّا بمتصفح فعلي** — وليس فقط بمراجعة الكود. تم أيضًا إعادة تشغيل كل اختبارات الوظائف السابقة (سلة، مفضلة، فلاتر متجر، فورمات، بحث، تنقل موبايل، حسابات مالية) بعد كل تعديل للتأكد من عدم كسر أي شيء يعمل حاليًا. **لم يتم حذف أو تغيير أي تصميم أو ميزة موجودة** — فقط إصلاح ما هو معطوب فعليًا.

---

## 7) مراجعة أمنية شاملة (Security Hardening)

تمت مراجعة كامل الكود بحثًا عن أي ثغرات أمنية، وهذا ما تم فحصه وإصلاحه:

**الثغرة المكتشفة والمُصلَحة — عدم تعقيم ثابت (Inconsistent HTML Escaping):**
- الكود كان يحتوي بالفعل على دالة تعقيم جاهزة (`escAttr`) ومُطبَّقة بشكل صحيح في أماكن حساسة (عرض السلة، المفضلة، النافذة السريعة، صفحة تفاصيل المنتج) — كلها كانت آمنة بالفعل.
- لكن في دالتين تحديدًا (عرض شبكة المنتجات بالصفحة الرئيسية، وعرض شبكة صفحة المتجر)، كانت بعض الحقول (اسم المنتج، رابط الصورة، التصنيف، السعر) تُدرَج مباشرة في HTML بدون تعقيم. تم توحيد التعقيم في كل مكان الآن، بدون أي تغيير مرئي (لأن البيانات الحالية لا تحتوي على رموز خاصة أصلاً — هذا تحصين وقائي لأي بيانات مستقبلية).

**نقاط أخرى تم التحقق منها وهي سليمة:**
- لا يوجد استخدام لـ `eval()` أو `new Function()` أو `document.write()` في أي مكان.
- كل روابط `window.open()` (فتح واتساب) تستخدم `noopener` بشكل صحيح (حماية من هجمات tabnabbing).
- رسائل واتساب يتم ترميزها بشكل صحيح (`encodeURIComponent`) قبل إرسالها في الرابط.
- رسائل التنبيه المنبثقة (toast) تُعرض عبر `textContent` الآمن، وليس HTML خام.
- لا توجد أي مفاتيح API أو كلمات مرور أو بيانات حساسة مكشوفة في الكود.
- لا توجد سكربتات خارجية محمَّلة من مصادر خارجية (CDN) — الموقع يعتمد فقط على ملفه الخاص `main.min.js`، فسطح الهجوم محدود أصلاً.
- كل روابط `target="_blank"` (لو وُجدت) مصحوبة بحماية مناسبة.

**طريقة التأكد:** بعد كل تعديل، تم التحقق من أن أسماء المنتجات ما زالت تظهر بشكل نظيف تمامًا (بدون أي رموز HTML ظاهرة مثل `&amp;`)، وأُعيد تشغيل كل اختبارات الوظائف السابقة للتأكد من عدم وجود أي تأثير بصري أو وظيفي.

---

## 9) إضافات جديدة (بناءً على طلب صاحب الموقع)

بعد الإصلاحات، تمت إضافة 7 تحسينات جديدة للموقع، كل واحدة اختُبرت حيًّا بمتصفح فعلي قبل اعتمادها، مع إعادة تشغيل كل اختبارات الوظائف السابقة بعد كل إضافة للتأكد من عدم كسر أي شيء يعمل حاليًا.

**أ) شريط تقدم الشحن المجاني** — في صفحة السلة، أصبح يظهر شريط تقدم بصري متحرك (بدرجات اللون الذهبي لهوية الموقع) يوضّح للزائر كم تبقّى لبلوغ حد الشحن المجاني (2,500 ج.م)، مع رسالة تهنئة عند بلوغه. البيانات نفسها كانت موجودة بالفعل في كود السلة، وتمت فقط إضافة التمثيل البصري.

**ب) "شوهد مؤخراً" (Recently Viewed)** — يظهر شريط بأحدث المنتجات التي تصفّحها الزائر (في صفحة المنتج وأسفل الصفحة الرئيسية)، باستخدام نفس آلية `localStorage` المستخدمة أصلاً في السلة والمفضلة، مع إعادة التحقق من السعر الحالي في كل مرة (نفس منطق الحماية من التلاعب بالسعر المذكور أعلاه) لضمان دقة الأسعار المعروضة دائمًا. يستثني المنتج الحالي من قائمته الخاصة، ولا يظهر إطلاقًا لزائر جديد ليس له تاريخ تصفّح بعد.

**ج) "أكمل الجو العام" (Complete the Look)** — تم تغيير منطق "المنتجات ذات الصلة" في صفحة المنتج: بدلاً من عرض المزيد من نفس التصنيف (مثلاً كنبة أخرى تحت كنبة)، يعرض الآن قطعًا من تصنيفات مكمّلة تناسب نفس المساحة (كنبة ← طاولة تلفزيون + إضاءة + ديكور، سفرة ← بوفيه + إضاءة، إلخ)، بعنوان جديد "أكمل الجو العام".

**د) مشاركة المنتج عبر واتساب** — زر جديد بجانب زر المفضلة في صفحة المنتج، يفتح واتساب برسالة جاهزة تحتوي اسم المنتج وسعره ورابط مباشر للمنتج نفسه.

**هـ) تحميل هيكلي (Skeleton Loading)** — كل صور المحتوى (باستثناء أيقونات الدفع الصغيرة) تُظهر تأثير نبض رمادي أنيق أثناء التحميل بدلاً من فراغ أو أيقونة صورة مكسورة، فيبدو الموقع أسرع استجابة. يعمل تلقائيًا مع كل الصور الحالية والمستقبلية دون الحاجة لتعديل كل قسم على حدة.

**و) تكبير الصورة عند التحويم (Zoom on Hover)** — في صفحة المنتج، تحريك الماوس فوق الصورة الرئيسية يكبّرها ويتتبّع موضع المؤشر، لإظهار تفاصيل الخامة والتشطيب بدقة أعلى. يعمل على الحواسيب فقط (لا يظهر على الموبايل حيث لا يوجد مؤشر فأر)، ولا يؤثر على إمكانية الضغط لفتح الصورة المكبّرة بالشاشة الكاملة.

**ز) تأثير الإمالة على بطاقات المنتج (Tilt Effect)** — إمالة ثلاثية الأبعاد خفيفة تتبع حركة الماوس عند التحويم على أي بطاقة منتج، في كل صفحات الموقع دفعة واحدة (الرئيسية، المتجر، شوهد مؤخراً، أكمل الجو العام، المفضلة) دون الحاجة لتعديل كل قسم بشكل منفصل.

---

## 10) تصحيح بخصوص قسم "آراء العملاء"

عند تنفيذ طلب إضافة قسم لآراء العملاء، تبيّن أن **هذا القسم موجود بالفعل** في الصفحة الرئيسية وصفحة "من نحن" (5 تقييمات بأسماء محددة). لوحظ أن كل تقييم كان مصحوبًا بعبارة "عميل موثّق" / "عميلة موثّقة" — وهي عبارة تؤكد صراحة أن الشخص عميل حقيقي تم التحقق منه. بما أنه لا توجد وسيلة للتأكد من صحة هذه التقييمات، فإن استخدام كلمة "موثّق" تحديدًا يحوّلها من محتوى نصي عام إلى **ادّعاء صريح قد يمثل مخاطرة قانونية/تسويقية** إذا لم تكن التقييمات موثّقة بالفعل.

**الإجراء المُتخذ:** تمت إزالة كلمة "موثّق/موثّقة" فقط (فبات النص "عميل"/"عميلة" بدلاً من "عميل موثّق"/"عميلة موثّقة")، مع الإبقاء على التقييمات والأسماء كما هي دون حذف أو تعديل، في كل من `index.html` و`about.html`. هذا إجراء احترازي بسيط يقلّل المخاطرة دون التأثير على المحتوى الفعلي، إلى حين مراجعة صاحب الموقع لمصدر هذه التقييمات وتأكيد صحتها أو استبدالها بتقييمات حقيقية موثّقة فعليًا.

---

## 8) حماية من التلاعب بالسعر أثناء الشراء (Price Tampering)

**المشكلة:** الموقع موقع ثابت (Static) بالكامل بدون أي سيرفر/قاعدة بيانات — السلة والمفضلة يتم حفظهما في `localStorage` بمتصفح الزائر نفسه. أي شخص يفتح أدوات المطوّر (F12) في متصفحه يقدر يعدّل السعر أو الخصم أو الكمية المحفوظة يدويًا قبل إتمام الطلب، وبما أن الموقع كان يثق بالكامل في البيانات المحفوظة، كان بيعرض وبيرسل في رسالة واتساب أي رقم يتم كتابته، حتى لو كان مزيَّفًا.

**الحل المُطبَّق:** تمت إضافة "قائمة أسعار مرجعية" (Catalog) تُبنى من نفس بيانات المنتجات الحقيقية المكتوبة في كود الموقع نفسه (وهي غير قابلة للتعديل من متصفح الزائر). الآن، في كل مرة تُقرأ فيها السلة أو المفضلة — سواء لعرضها على الشاشة، حساب الإجمالي، أو تجهيز رسالة واتساب — يتم **التحقق من كل منتج مقابل هذه القائمة المرجعية تلقائيًا**:
- لو السعر أو الخصم في الذاكرة المحفوظة مختلف عن السعر الحقيقي الحالي للمنتج، يتم **استبداله فورًا وبصمت بالسعر الصحيح**، في كل من الشاشة **ورسالة واتساب المُرسَلة فعليًا للتاجر**.
- تم أيضًا تحديد حد أقصى معقول للكمية (1 إلى 99 قطعة لكل منتج) لمنع محاولات إدخال كميات وهمية (مثل 99999) للتأثير على الحسابات.
- أي منتج لا يوجد له تطابق في القائمة المرجعية (مثلاً منتج قديم تم حذفه من الموقع) **يبقى كما هو بدون أي تغيير** — الحماية لا تكسر أي حالة استخدام شرعية.

**تم اختبار هذا فعليًا بمحاكاة هجوم حقيقي:** تم حقن منتج حقيقي (أريكة نور المخملية، سعرها الحقيقي 15,500 ج.م) في localStorage بسعر مزيَّف (1 جنيه فقط)، وخصم وهمي (99%)، وكمية 500 قطعة. النتيجة: كل من الشاشة ورسالة واتساب الفعلية عرضا السعر الصحيح **15,500 ج.م** وخصمه الحقيقي **19%**، والكمية اتحددت تلقائيًا عند **99**. تم اختبار عكس ذلك أيضًا (منتج غير موجود بالكتالوج) للتأكد أنه يبقى شغالًا بشكل طبيعي بدون أي كسر.

**ملاحظة أمانة يجب معرفتها:** هذا الحل يمنع الطريقة الأكثر شيوعًا وسهولة للتلاعب (تعديل localStorage عبر أدوات المطوّر) بشكل كامل. لكن بما أن الموقع بالكامل يعمل من متصفح الزائر بدون أي خادم يتحقق من الطلب، تبقى هناك حدود نظرية لأي حل من جهة العميل فقط (Client-Side) — لو أراد شخص متمرّس تقنيًا تعديل تنفيذ الصفحة الحي نفسه (وليس فقط البيانات المحفوظة)، فهذا خارج نطاق ما يمكن لأي موقع ثابت منعه بالكامل. الحل الجذري النهائي لهذا يتطلب نظام خلفي (Backend) يتحقق من السعر وقت تأكيد الدفع الفعلي — وهو تغيير أكبر في البنية يمكن مناقشته لاحقًا لو رغبتم في ذلك مستقبلًا. الوضع الحالي يغطي التهديد العملي الأكثر شيوعًا لهذا النوع من المواقع بشكل كامل.
